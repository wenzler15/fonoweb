import {
  __commonJS
} from "./chunk-J43GMYXM.js";

// node_modules/yup-locale-pt/lib/printValue.js
var require_printValue = __commonJS({
  "node_modules/yup-locale-pt/lib/printValue.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", {
      value: true
    });
    exports.default = printValue;
    var {
      toString
    } = Object.prototype;
    var errorToString = Error.prototype.toString;
    var regExpToString = RegExp.prototype.toString;
    var symbolToString = typeof Symbol !== "undefined" ? Symbol.prototype.toString : () => "";
    var SYMBOL_REGEXP = /^Symbol\((.*)\)(.*)$/;
    function printNumber(val) {
      if (val != +val)
        return "NaN";
      const isNegativeZero = val === 0 && 1 / val < 0;
      return isNegativeZero ? "-0" : `${val}`;
    }
    function printSimpleValue(val, quoteStrings = false) {
      if (val == null || val === true || val === false)
        return `${val}`;
      const typeOf = typeof val;
      if (typeOf === "number")
        return printNumber(val);
      if (typeOf === "string")
        return quoteStrings ? `"${val}"` : val;
      if (typeOf === "function") {
        return `[Function ${val.name || "anonymous"}]`;
      }
      if (typeOf === "symbol") {
        return symbolToString.call(val).replace(SYMBOL_REGEXP, "Symbol($1)");
      }
      const tag = toString.call(val).slice(8, -1);
      if (tag === "Date") {
        return isNaN(val.getTime()) ? `${val}` : val.toISOString(val);
      }
      if (tag === "Error" || val instanceof Error) {
        return `[${errorToString.call(val)}]`;
      }
      if (tag === "RegExp")
        return regExpToString.call(val);
      return null;
    }
    function printValue(value, quoteStrings) {
      const result = printSimpleValue(value, quoteStrings);
      if (result !== null)
        return result;
      return JSON.stringify(value, function(key, value2) {
        const result2 = printSimpleValue(this[key], quoteStrings);
        if (result2 !== null)
          return result2;
        return value2;
      }, 2);
    }
  }
});

// node_modules/yup-locale-pt/lib/locale.js
var require_locale = __commonJS({
  "node_modules/yup-locale-pt/lib/locale.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", {
      value: true
    });
    exports.string = exports.object = exports.number = exports.mixed = exports.default = exports.date = exports.boolean = exports.array = void 0;
    var _printValue = _interopRequireDefault(require_printValue());
    function _interopRequireDefault(obj) {
      return obj && obj.__esModule ? obj : { default: obj };
    }
    var mixed = {
      default: "${path} \xE9 inv\xE1lido",
      required: "${path} \xE9 obrigat\xF3rio",
      oneOf: "${path} deve ter um dos seguintes valores: ${values}",
      notOneOf: "${path} n\xE3o deve ter nenhum dos seguintes valores: ${values}",
      notType: ({
        path,
        type,
        value,
        originalValue
      }) => {
        const isCast = originalValue != null && originalValue !== value;
        let msg = `${`${path} deve ser do tipo \`${type}\`, mas o valor final \xE9: \`${(0, _printValue.default)(value, true)}\``}${isCast ? ` (cast do valor \`${(0, _printValue.default)(originalValue, true)}\`)` : ""}`;
        if (value === null) {
          msg += '\nse a inten\xE7\xE3o era usar "null" como um valor em branco marque o esquema como `.nullable()`';
        }
        return msg;
      },
      defined: "${path} n\xE3o deve ser indefinido"
    };
    exports.mixed = mixed;
    var string = {
      length: ({
        path,
        length
      }) => `${path} deve ter exatamente ${length} ${length === 1 ? "caractere" : "caracteres"}`,
      min: ({
        path,
        min
      }) => `${path} deve ter pelo menos ${min} ${min === 1 ? "caractere" : "caracteres"}`,
      max: ({
        path,
        max
      }) => `${path} deve ter no m\xE1ximo ${max} ${max === 1 ? "caractere" : "caracteres"}`,
      matches: '${path} deve corresponder ao padr\xE3o: "${regex}"',
      email: "${path} deve ser um e-mail v\xE1lido",
      url: "${path} deve ser uma URL v\xE1lida",
      trim: "${path} n\xE3o deve conter espa\xE7os adicionais no in\xEDcio nem no fim",
      lowercase: "${path} deve estar em letras min\xFAsculas",
      uppercase: "${path} deve estar em letras mai\xFAsculas"
    };
    exports.string = string;
    var number = {
      min: "${path} deve ser maior ou igual a ${min}",
      max: "${path} deve menor ou igual a ${max}",
      lessThan: "${path} deve ser menor que ${less}",
      moreThan: "${path} deve ser maior que ${more}",
      notEqual: "${path} n\xE3o deve ser igual a ${notEqual}",
      positive: "${path} deve ser um n\xFAmero positivo",
      negative: "${path} deve ser um n\xFAmero negativo",
      integer: "${path} deve ser um n\xFAmero inteiro"
    };
    exports.number = number;
    var date = {
      min: "${path} deve ser posterior a ${min}",
      max: "${path} deve ser anterior a ${max}"
    };
    exports.date = date;
    var boolean = {};
    exports.boolean = boolean;
    var object = {
      noUnknown: "${path} tem chaves desconhecidas: ${unknown}"
    };
    exports.object = object;
    var array = {
      min: ({
        path,
        min
      }) => `${path} deve ter pelo menos ${min} ${min === 1 ? "item" : "itens"}`,
      max: ({
        path,
        max
      }) => `${path} deve ter no m\xE1ximo ${max} ${max === 1 ? "item" : "itens"}`
    };
    exports.array = array;
    var _default = {
      mixed,
      string,
      number,
      date,
      object,
      array,
      boolean
    };
    exports.default = _default;
  }
});

// node_modules/yup-locale-pt/lib/localeForm.js
var require_localeForm = __commonJS({
  "node_modules/yup-locale-pt/lib/localeForm.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", {
      value: true
    });
    exports.string = exports.object = exports.number = exports.mixed = exports.default = exports.date = exports.boolean = exports.array = void 0;
    var _printValue = _interopRequireDefault(require_printValue());
    function _interopRequireDefault(obj) {
      return obj && obj.__esModule ? obj : { default: obj };
    }
    var mixed = {
      default: "O campo \xE9 inv\xE1lido.",
      required: "O campo \xE9 obrigat\xF3rio.",
      oneOf: "O campo deve ter um dos seguintes valores: ${values}.",
      notOneOf: "O campo n\xE3o deve ter nenhum dos seguintes valores: ${values}.",
      notType: ({
        type,
        value,
        originalValue
      }) => {
        const isCast = originalValue != null && originalValue !== value;
        let msg = `${`O campo deve ser do tipo \`${type}\`, mas o valor final \xE9: \`${(0, _printValue.default)(value, true)}\``}${isCast ? ` (cast do valor \`${(0, _printValue.default)(originalValue, true)}\`).` : "."}`;
        if (value === null) {
          msg += '\nSe a inten\xE7\xE3o era usar "null" como um valor em branco marque o esquema como `.nullable()`.';
        }
        return msg;
      },
      defined: "O campo n\xE3o deve ser indefinido."
    };
    exports.mixed = mixed;
    var string = {
      length: ({
        length
      }) => `O campo deve ter exatamente ${length} ${length === 1 ? "caractere" : "caracteres"}.`,
      min: ({
        min
      }) => `O campo deve ter pelo menos ${min} ${min === 1 ? "caractere" : "caracteres"}.`,
      max: ({
        max
      }) => `O campo deve ter no m\xE1ximo ${max} ${max === 1 ? "caractere" : "caracteres"}.`,
      matches: 'O campo deve corresponder ao padr\xE3o: "${regex}".',
      email: "O campo deve ser um e-mail v\xE1lido.",
      url: "O campo deve ser uma URL v\xE1lida.",
      trim: "O campo n\xE3o deve conter espa\xE7os adicionais no in\xEDcio nem no fim.",
      lowercase: "O campo deve estar em letras min\xFAsculas.",
      uppercase: "O campo deve estar em letras mai\xFAsculas."
    };
    exports.string = string;
    var number = {
      min: "O campo deve ser maior ou igual a ${min}.",
      max: "O campo deve menor ou igual a ${max}.",
      lessThan: "O campo deve ser menor que ${less}.",
      moreThan: "O campo deve ser maior que ${more}.",
      notEqual: "O campo n\xE3o deve ser igual a ${notEqual}.",
      positive: "O campo deve ser um n\xFAmero positivo.",
      negative: "O campo deve ser um n\xFAmero negativo.",
      integer: "O campo deve ser um n\xFAmero inteiro."
    };
    exports.number = number;
    var date = {
      min: "O campo deve ser posterior a ${min}.",
      max: "O campo deve ser anterior a ${max}."
    };
    exports.date = date;
    var boolean = {};
    exports.boolean = boolean;
    var object = {
      noUnknown: "O campo tem chaves desconhecidas: ${unknown}."
    };
    exports.object = object;
    var array = {
      min: ({
        min
      }) => `O campo deve ter pelo menos ${min} ${min === 1 ? "item" : "itens"}.`,
      max: ({
        max
      }) => `O campo deve ter no m\xE1ximo ${max} ${max === 1 ? "item" : "itens"}.`
    };
    exports.array = array;
    var _default = {
      mixed,
      string,
      number,
      date,
      object,
      array,
      boolean
    };
    exports.default = _default;
  }
});

// node_modules/yup-locale-pt/lib/localeShort.js
var require_localeShort = __commonJS({
  "node_modules/yup-locale-pt/lib/localeShort.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", {
      value: true
    });
    exports.string = exports.object = exports.number = exports.mixed = exports.default = exports.date = exports.boolean = exports.array = void 0;
    var _printValue = _interopRequireDefault(require_printValue());
    function _interopRequireDefault(obj) {
      return obj && obj.__esModule ? obj : { default: obj };
    }
    var mixed = {
      default: "Inv\xE1lido.",
      required: "Obrigat\xF3rio.",
      oneOf: "Deve ter um dos seguintes valores: ${values}.",
      notOneOf: "N\xE3o deve ter nenhum dos seguintes valores: ${values}.",
      notType: ({
        type,
        value,
        originalValue
      }) => {
        const isCast = originalValue != null && originalValue !== value;
        let msg = `${`Deve ser do tipo \`${type}\`, mas o valor final \xE9: \`${(0, _printValue.default)(value, true)}\``}${isCast ? ` (cast do valor \`${(0, _printValue.default)(originalValue, true)}\`).` : "."}`;
        if (value === null) {
          msg += '\nSe a inten\xE7\xE3o era usar "null" como um valor em branco marque o esquema como `.nullable()`.';
        }
        return msg;
      },
      defined: "N\xE3o deve ser indefinido."
    };
    exports.mixed = mixed;
    var string = {
      length: ({
        length
      }) => `Deve ter exatamente ${length} ${length === 1 ? "caractere" : "caracteres"}.`,
      min: ({
        min
      }) => `Deve ter pelo menos ${min} ${min === 1 ? "caractere" : "caracteres"}.`,
      max: ({
        max
      }) => `Deve ter no m\xE1ximo ${max} ${max === 1 ? "caractere" : "caracteres"}.`,
      matches: 'Deve corresponder ao padr\xE3o: "${regex}".',
      email: "Deve ser um e-mail v\xE1lido.",
      url: "Deve ser uma URL v\xE1lida.",
      trim: "N\xE3o deve conter espa\xE7os adicionais no in\xEDcio nem no fim.",
      lowercase: "Deve estar em letras min\xFAsculas.",
      uppercase: "Deve estar em letras mai\xFAsculas."
    };
    exports.string = string;
    var number = {
      min: "Deve ser maior ou igual a ${min}.",
      max: "Deve ser menor ou igual a ${max}.",
      lessThan: "Deve ser menor que ${less}.",
      moreThan: "Deve ser maior que ${more}.",
      notEqual: "N\xE3o deve ser igual a ${notEqual}.",
      positive: "Deve ser um n\xFAmero positivo.",
      negative: "Deve ser um n\xFAmero negativo.",
      integer: "Deve ser um n\xFAmero inteiro."
    };
    exports.number = number;
    var date = {
      min: "Deve ser posterior a ${min}.",
      max: "Deve ser anterior a ${max}."
    };
    exports.date = date;
    var boolean = {};
    exports.boolean = boolean;
    var object = {
      noUnknown: "Existem chaves desconhecidas: ${unknown}."
    };
    exports.object = object;
    var array = {
      min: ({
        min
      }) => `Deve ter pelo menos ${min} ${min === 1 ? "item" : "itens"}.`,
      max: ({
        max
      }) => `Deve ter no m\xE1ximo ${max} ${max === 1 ? "item" : "itens"}.`
    };
    exports.array = array;
    var _default = {
      mixed,
      string,
      number,
      date,
      object,
      array,
      boolean
    };
    exports.default = _default;
  }
});

// node_modules/yup-locale-pt/lib/index.js
var require_lib = __commonJS({
  "node_modules/yup-locale-pt/lib/index.js"(exports) {
    Object.defineProperty(exports, "__esModule", {
      value: true
    });
    exports.ptShort = exports.ptForm = exports.pt = exports.default = void 0;
    var _locale = _interopRequireDefault(require_locale());
    var _localeForm = _interopRequireDefault(require_localeForm());
    var _localeShort = _interopRequireDefault(require_localeShort());
    function _interopRequireDefault(obj) {
      return obj && obj.__esModule ? obj : { default: obj };
    }
    var pt = _locale.default;
    exports.pt = pt;
    var ptForm = _localeForm.default;
    exports.ptForm = ptForm;
    var ptShort = _localeShort.default;
    exports.ptShort = ptShort;
    var _default = {
      pt,
      ptForm,
      ptShort
    };
    exports.default = _default;
  }
});
export default require_lib();
//# sourceMappingURL=yup-locale-pt.js.map
