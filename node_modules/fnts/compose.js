"use strict";
/**
 * @module Composition
 */
Object.defineProperty(exports, "__esModule", { value: true });
/**
 * Applies all of the provided `functions` one-by-one in right-to-left order.
 */
function compose() {
    var functions = [];
    for (var _i = 0; _i < arguments.length; _i++) {
        functions[_i] = arguments[_i];
    }
    return function () {
        var args = [];
        for (var _i = 0; _i < arguments.length; _i++) {
            args[_i] = arguments[_i];
        }
        var length = functions.length;
        var composition = functions[length - 1].apply(functions, args);
        if (length > 1) {
            for (var index = length - 2; index >= 0; index -= 1) {
                composition = functions[index](composition);
            }
        }
        return composition;
    };
}
exports.default = compose;
